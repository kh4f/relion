{{~#>header~}}
	## &ensp; [` 📦 {{tag}}  `]({{>compareLink}}){{>br}}
{{/header}}

{{~#>main~}}
	{{#each commitTypeGroups~}}
		### {{{repeat '&nbsp;' 5}}}{{title}}
		{{#if (eq @key 'breaking')}}
			{{#each commits}}
				{{#if (isArray breakingChanges)}}
					{{~#each breakingChanges~}}
						- {{{this}}} {{#if (isBreakingCommitInOtherTypeGroup ../this)}}<sup>[{{../breakingChangeIndex}}]</sup>{{/if}}
					{{/each}}
				{{else~}}
					- {{{breakingChanges}}} {{#if (isBreakingCommitInOtherTypeGroup this)}}<sup>[{{breakingChangeIndex}}]</sup>{{/if}}
				{{/if}}
			{{/each}}
		{{else}}
			{{#if scopeGroups}}
				{{#each scopeGroups~}}
					{{~#if (or (isSingle commits) (not scope))}}
						{{#each commits~}}
							- {{>scope}}{{>commit}}
						{{/each}}
					{{else~}}
						- {{>scope}}{{>br}}{{~#each commits}}  {{" "~}}
							- {{>commit}}
						{{/each}}
					{{/if}}
				{{/each}}
			{{else}}
				{{#each commits~}}
					- {{>scope}}{{>commit}}
				{{/each}}
			{{/if}}
		{{/if}}

	{{/each}}
{{/main}}

{{~#>footer~}}
	##### &emsp;&ensp;&nbsp;&nbsp; [_Release Changelog_]({{>changelogUrl}}) &ensp;•&ensp; [_All Release Commits_]({{>compareLink}}) &ensp;•&ensp; _{{date}}_{{>br}}{{>br}}
{{/footer}}